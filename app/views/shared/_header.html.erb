<nav class="navbar navbar-default navbar-fixed-top" ng-controller="HeaderCtrl">
    <div class="container">

        <div class="navbar-header">
            <button class="navbar-toggle collapsed">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">GolyGlot</a>
        </div>

        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <% if current_user %>
                    <li>
                        <%= link_to user_lexical_resources_path(current_user) do %>
                            <%= current_user.name %>
                        <% end %>
                    </li>
                    <li>
                        <a href="">
                            <span class="glyphicon glyphicon-plus" aria-hidden="true"></span>
                        </a>
                    </li>
                    <li>
                        <a href="">
                            <span class="glyphicon glyphicon-cog" aria-hidden="true"></span>
                        </a>
                    </li>
                    <li>
                        <%= link_to log_out_path do %>
                            <span class="glyphicon glyphicon-log-out" aria-hidden="true"></span>
                        <% end %>
                    </li>
                <% else %>
                    <li>
                        <a href="">Guest</a>
                    </li>
                    <li>
                        <form class="navbar-form pull-left" action="<%= sign_up_path %>">
                            <button type="submit" class="btn btn-success">Sign Up</button>
                        </form>
                        <form class="navbar-form pull-left" action="<%= log_in_path %>">
                            <button type="submit" class="btn btn-default">Sign In</button>
                        </form>
                    </li>
                <% end %>
            </ul>
        </div>

    </div>
        
</nav>



